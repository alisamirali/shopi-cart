import { Search } from "lucide-react";

const SearchBar = () => {
  return (
    <Search className="size-6 hover:text-shop_light_green hoverEffect cursor-pointer" />
  );
};

export default SearchBar;
